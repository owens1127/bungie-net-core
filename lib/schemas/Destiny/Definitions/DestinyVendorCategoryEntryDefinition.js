/**
 * Bungie.Net API
 * These endpoints constitute the functionality exposed by Bungie.net, both for more traditional website functionality and for connectivity to Bungie video games and their related functionality.
 *
 * OpenAPI spec version: 2.16.0
 * Contact: support@bungie.com
 *
 * NOTE: This class is auto generated by the bungie-api-typedef code generator program,
 * adapted from {@link https://github.com/DestinyItemManager/bungie-api-ts}
 * Repository: {@link https://github.com/owensimpson/oodestiny}
 * Do not edit these files manually.
 */

/**
 * This is the definition for a single Vendor Category, into which Sale Items are
 * grouped.
 * @type DestinyVendorCategoryEntryDefinition
 * @property {number} categoryIndex The index of the category in the original category definitions for the vendor.
 * @property {number} sortValue Used in sorting items in vendors... but there's a lot more to it. Just go with the order provided in the itemIndexes property on the DestinyVendorCategoryComponent instead, it should be more reliable than trying to recalculate it yourself.
 * @property {number} categoryHash The hashed identifier for the category.
 * @property {number} quantityAvailable The amount of items that will be available when this category is shown.
 * @property {boolean} showUnavailableItems If items aren't up for sale in this category, should we still show them (greyed out)?
 * @property {boolean} hideIfNoCurrency If you don't have the currency required to buy items from this category, should the items be hidden?
 * @property {boolean} hideFromRegularPurchase True if this category doesn't allow purchases.
 * @property {string} buyStringOverride The localized string for making purchases from this category, if it is different from the vendor's string for purchasing.
 * @property {string} disabledDescription If the category is disabled, this is the localized description to show.
 * @property {string} displayTitle The localized title of the category.
 * @property {DestinyVendorCategoryOverlayDefinition} overlay If this category has an overlay prompt that should appear, this contains the details of that prompt.
 * @property {number[]} vendorItemIndexes A shortcut for the vendor item indexes sold under this category. Saves us from some expensive reorganization at runtime.
 * @property {boolean} isPreview Sometimes a category isn't actually used to sell items, but rather to preview them. This implies different UI (and manual placement of the category in the UI) in the game, and special treatment.
 * @property {boolean} isDisplayOnly If true, this category only displays items: you can't purchase anything in them.
 * @property {number} resetIntervalMinutesOverride 
 * @property {number} resetOffsetMinutesOverride 
 * @see {@link https://bungie-net.github.io/#/components/schemas/Destiny.Definitions.DestinyVendorCategoryEntryDefinition}
*/
class DestinyVendorCategoryEntryDefinition {
  /** @readonly */
  categoryIndex;
  /** @readonly */
  sortValue;
  /** @readonly */
  categoryHash;
  /** @readonly */
  quantityAvailable;
  /** @readonly */
  showUnavailableItems;
  /** @readonly */
  hideIfNoCurrency;
  /** @readonly */
  hideFromRegularPurchase;
  /** @readonly */
  buyStringOverride;
  /** @readonly */
  disabledDescription;
  /** @readonly */
  displayTitle;
  /** @readonly */
  overlay;
  /** @readonly */
  vendorItemIndexes;
  /** @readonly */
  isPreview;
  /** @readonly */
  isDisplayOnly;
  /** @readonly */
  resetIntervalMinutesOverride;
  /** @readonly */
  resetOffsetMinutesOverride;
}
module.exports = DestinyVendorCategoryEntryDefinition;
